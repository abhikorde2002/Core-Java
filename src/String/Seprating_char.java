package String;

public class Seprating_char {
    public static void main(String[] args) {
        String str = "Pentagon";
        char ch;
        for (int i = 0; i < str.length(); i++) {
            ch = str.charAt(i);

            System.out.print(ch + " ");
        }

    }
}
